export { TerminalService } from './terminal-service';
export { TerminalView, TERMINAL_VIEW_TYPE } from './terminal-view';
export { DEFAULT_TERMINAL_SETTINGS } from './types';
export type { TerminalSettings, TerminalOpenPosition, TerminalSessionState } from './types';
